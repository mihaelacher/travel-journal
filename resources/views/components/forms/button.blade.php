<div class="row">
    <button type="{{$type}}" value="{{$value}}" class="btn {{$classType}}">
        @if($icon)
            <i class="material-icons">{{$icon}}</i>
        @endif
    </button>
</div>
